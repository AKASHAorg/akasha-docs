{
  // This file is not used in compilation. It is here just for a nice editor experience.
  "extends": "@docusaurus/tsconfig/tsconfig.json",
  "exclude": ["node_modules", "build", "dist", ".docusaurus", ".yarn", "akasha-core"],
  "compilerOptions": {
    "baseUrl": "."
  }
}
