# Publishing extension is easy-peasy

-- first enable dev-mode

-- create app in UI
... add description ...

-- after creating the app in the UI,
-- run npm init and set the name exactly the same as in the UI
